#importando la funcion del modlulo con (as) podemos cambiar el nombre
#con import importamos la funcion
#con fron (modulo) import (funcion especifica) improtamos solo la funcion

from modulos_saludar import saludar# para importar solo la funcion si queremos importar mas de una se usa ,
#usando la funcion importada
saluda = saludar(" ")
print(saluda)